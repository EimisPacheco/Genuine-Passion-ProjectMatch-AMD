# Lean runtime deps for the App Runner container. Heavy/optional packages
# (sentence-transformers/torch, pyttsx3,
# Pillow) are omitted — they're all lazy-imported and degrade gracefully
# (embeddings fall back to a hash vector; Cloud SQL pgvector still does the search).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
sse-starlette==2.2.1
httpx==0.28.1
requests>=2.31
openai>=1.30.0
langgraph==0.2.60
langchain-core==0.3.28
psycopg[binary]>=3.1
pgvector>=0.2.5
beautifulsoup4==4.12.3
lxml==5.3.0
