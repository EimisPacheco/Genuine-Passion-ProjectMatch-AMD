# --- Core API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
sse-starlette==2.2.1
httpx==0.28.1

# --- Agents / LLM ---
# All providers (AMD/Ollama, Google) speak the OpenAI-compatible
# API, so one client covers them all.
openai>=1.30.0
langgraph==0.2.60
langchain-core==0.3.28

# --- Sponsor platforms ---
# GCP Cloud SQL for PostgreSQL + pgvector (database backend)
psycopg[binary]>=3.1
pgvector>=0.2.5
langfuse==2.57.0

# --- Embeddings (local, no key) ---
sentence-transformers==3.3.1

# --- Scraping ---
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Video / TTS ---
pyttsx3==2.98
Pillow==11.0.0

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.0
